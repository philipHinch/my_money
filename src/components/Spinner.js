const Spinner = () => {
    return (
        <div className="loadingSpinnerContainer" style={{ height: '100vh' }}>
            <div className="loadingSpinner" ></div>
        </div>
    );
}

export default Spinner;